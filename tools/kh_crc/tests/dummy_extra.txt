# kmod/exports.manifest — KernelHook freestanding export table, v1-reset.
#
# Reset on 2026-04-15 as part of the kh_ public-API namespace turn (P3).
# The prior v1 APPEND-ONLY contract is dropped — no external consumer
# was pinned to the pre-reset CRCs. Going forward: APPEND-ONLY again
# (the reset point is the baseline).
#
# Format: <name> : <ret-token> : <args>
#   <args> is a comma-separated list of argument tokens, or empty for no args.
#
# Tokens (human-readable layer):
#   void, i32, u32, enum, i64, u64, uptr, ptr, pptr
# Each maps to an ABI class {v, w, x}; see kh_crc abi_class() for the mapping.
# CRC canonical string: "<name>(<arg_classes>)-><ret_class>"
# CRC32: IEEE 802.3, poly 0xedb88320, init ~0, xorout ~0.

kh_hook_prepare                : enum : ptr
kh_hook_install                : void : ptr
kh_hook_uninstall              : void : ptr
kh_hook                        : enum : ptr, ptr, pptr
kh_unhook                      : void : ptr
kh_hook_chain_add              : enum : ptr, ptr, ptr, ptr, i32
kh_hook_chain_remove           : void : ptr, ptr, ptr
kh_hook_wrap                   : enum : ptr, i32, ptr, ptr, ptr, i32
kh_hook_unwrap_remove          : void : ptr, ptr, ptr, i32
kh_hook_chain_setup_transit    : void : ptr
kh_fp_hook                     : void : uptr, ptr, pptr
kh_fp_unhook                   : void : uptr, ptr
kh_fp_hook_wrap                : enum : uptr, i32, ptr, ptr, ptr, i32
kh_fp_hook_unwrap              : void : uptr, ptr, ptr
kh_fp_hook_chain_setup_transit : void : ptr
ksyms_lookup                   : u64  : ptr

# Contract 1 test: this extra line must NOT change any other symbol's CRC.
dummy_never_exported : void : i32
